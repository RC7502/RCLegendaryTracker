@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>
@using (@Html.BeginForm("LoginSubmit", "Account"))
{
    @Html.TextBox("username")<br/>
    @Html.Password("password")<br/>
    <input type="submit" value="Login" />
}